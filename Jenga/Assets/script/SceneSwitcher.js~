#pragma strict

function Update () {
	/*
	if(Input.GetMouseButtonDown(0)){
		//マウスがクリックされた
		if (Application.loadedLevelName == "Top") {
			//現在シーン１を表示中なので，シーン２に切り替える
			Application.LoadLevel("FB");
		} else {
			//現在シーン２を表示中なので，シーン１に切り替える
			Application.LoadLevel("Top");
		}
	}
	*/
}
