#pragma strict

function Start () {

}

function Update () {
	var x : float = Input.GetAxis("Horizontal");
	var vec3 : Vector3 = Vector3(0, x*2, 0);
	transform.RotateAround(vec3.zero, vec3.up, -vec3.y);
	
}
